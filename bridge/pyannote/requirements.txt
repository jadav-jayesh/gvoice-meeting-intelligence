# gVoice Pyannote diarization bridge (pyannote.ai cloud API).
# No ML deps — this is a thin async HTTP wrapper around the hosted API.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
httpx==0.28.1
